---
apiVersion: v1
kind: Service
metadata:
  name: hello-svc-cip
  #annotations:
  #  external-dns.alpha.kubernetes.io/hostname: hello-svc-cip.cloud.holisticsecurity.io
  labels:
    app: hello
  namespace: hello
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 5080
    targetPort: 5000
    protocol: TCP
  selector:
    app: hello
---
apiVersion: v1
kind: Service
metadata:
  name: hello-svc-lb
  #annotations:
  #  external-dns.alpha.kubernetes.io/hostname: hello-svc-lb.cloud.holisticsecurity.io
  labels:
    app: hello
  namespace: hello
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 5080
    targetPort: 5000
    protocol: TCP
  selector:
    app: hello
---
apiVersion: v1
kind: Service
metadata:
  name: hello-svc-np
  annotations:
    external-dns.alpha.kubernetes.io/hostname: hello-svc-np.cloud.holisticsecurity.io
  labels:
    app: hello
  namespace: hello
spec:
  type: NodePort
  ports:
  - name: http
    port: 5080
    targetPort: 5000
    protocol: TCP
  selector:
    app: hello
---